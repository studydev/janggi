# 웹 장기(將棋) 게임 개발 프롬프트 모음

**P0 → P1을 먼저 고정**하고, 나머지는 위에서부터 하나씩 진행하세요.

기본 전제 (다르면 프롬프트에서 바꿔 쓰세요)
- 스택: TypeScript + React + Vite, 렌더링은 SVG
- 규칙 엔진은 UI와 완전히 분리된 순수 TS 모듈
- 온라인 대전(P12)과 AI 상대(P10)는 선택 모듈

---

## ⚠️ 가장 중요한 주의사항

**장기(Janggi)를 중국 샹치(Xiangqi)로 착각해서 코드를 짜는 경우가 매우 많습니다.**
아래 차이를 프롬프트에 매번 명시하세요.

| 항목 | 장기 (한국) | 샹치 (중국) — 이렇게 짜면 오답 |
|---|---|---|
| 강(河) | **없음** | 강이 있고 상은 못 건넘 |
| 상(象) | 1칸 직진 + 2칸 대각 (총 3칸) | 2칸 대각만 |
| 포(包) | **이동·공격 모두 반드시 1개를 넘어야 함** | 이동은 차처럼, 공격만 넘음 |
| 포끼리 | 포를 넘을 수 없고 포를 잡을 수도 없음 | 제한 없음 |
| 졸/병 | 처음부터 옆으로 이동 가능, 뒤로는 불가 | 강 건너야 옆 이동 |
| 차·왕·사 | 궁성 안에서는 대각선 이동 가능 | 대각선 없음 |
| 한 수 쉬기 | **허용됨** | 불가 |
| 초기 배치 | 마·상 위치를 대국 전에 선택 | 고정 |

---

## P0. 프로젝트 헌법 (CLAUDE.md 또는 첫 프롬프트)

```
웹 기반 장기(Korean Chess, Janggi) 게임을 만든다. 아래를 프로젝트 전체 규칙으로 삼는다.

[기술 스택]
- TypeScript(strict), React 18, Vite
- 상태관리: 외부 라이브러리 없이 useReducer + Context (규모 커지면 zustand)
- 보드 렌더링: SVG
- 테스트: Vitest

[아키텍처 원칙]
- src/engine/ : 순수 함수만. React, DOM, 브라우저 API 참조 금지. 이 폴더만으로 콘솔에서 대국이 가능해야 한다.
- src/ui/     : 렌더링과 입력 처리. 규칙 판단 로직을 절대 두지 않는다.
- src/ai/     : 엔진에만 의존.
- 엔진은 불변(immutable) 상태로 다룬다. makeMove는 새 GameState를 반환한다.

[작업 방식]
- 한 번에 하나의 모듈만 구현한다. 요청하지 않은 파일은 건드리지 않는다.
- 규칙 로직은 구현 전에 테스트 케이스를 먼저 작성한다.
- 규칙이 애매하면 추측하지 말고 나에게 질문한다.
- RULES.md에 적힌 규칙이 항상 최우선이다. 다른 체스류 게임의 상식으로 보완하지 않는다.

[금지]
- 장기를 중국 샹치 규칙으로 구현하지 말 것. 강(河)은 없고, 상은 1직진+2대각, 포는 이동할 때도 반드시 하나를 넘는다.
```

---

## P1. 규칙 명세서 고정 (RULES.md)

> 이 프롬프트가 프로젝트 품질의 80%를 결정합니다. 그대로 붙여넣어 `RULES.md`로 저장시키세요.

```
아래 내용을 그대로 RULES.md 파일로 저장해라. 내용을 요약하거나 수정하지 마라.
앞으로 모든 규칙 판단은 이 파일을 근거로 한다.

# 장기 규칙 명세

## 보드
- 9열 × 10행. 기물은 칸이 아니라 선의 교차점에 놓인다.
- 좌표: file 1~9 (왼쪽→오른쪽), rank 1~10 (위→아래).
- 궁성(宮): 한(漢) = file 4~6 / rank 1~3, 초(楚) = file 4~6 / rank 8~10.
  각 궁성에는 네 귀퉁이와 중앙을 잇는 대각선이 그어져 있다.
- 강(河)은 존재하지 않는다.

## 초기 배치
- 한(漢)이 위쪽(rank 1~4), 초(楚)가 아래쪽(rank 7~10).
- 한: rank1 = 차(1) 마/상(2) 마/상(3) 사(4) 빈(5) 사(6) 마/상(7) 마/상(8) 차(9)
      rank2 = 궁(5) / rank3 = 포(2), 포(8) / rank4 = 병(1,3,5,7,9)
- 초: 위 배치를 상하 대칭. rank10 = 차·사·차, rank9 = 궁(5), rank8 = 포(2,8), rank7 = 졸(1,3,5,7,9)
- 마·상 배치는 대국 시작 전 각자 고른다: 마상마상 / 상마상마 / 마상상마 / 상마마상
- 초(楚)가 선수(먼저 둔다).

## 기물별 이동
- 궁(將): 궁성 안에서만 선을 따라 1칸. 궁성 대각선 위에서는 대각으로도 1칸.
- 사(士): 궁과 동일. 궁성을 벗어날 수 없다.
- 차(車): 가로·세로로 막힐 때까지 직진. 궁성 안 대각선 위에 있으면 그 대각선을 따라 직진 가능.
- 포(包): 이동과 공격 모두, 사이에 정확히 기물 1개(포대)를 넘어야 한다.
  - 포대가 0개거나 2개 이상이면 그 방향으로 갈 수 없다.
  - 포는 다른 포를 넘을 수 없다. 포는 다른 포를 잡을 수 없다.
  - 궁성 대각선에서도 같은 조건(궁성 중앙에 포대가 있으면)으로 이동 가능.
- 마(馬): 직선 1칸 + 대각 1칸. 첫 직선 칸에 기물이 있으면 막혀서 못 간다(다리 막힘).
- 상(象): 직선 1칸 + 대각 2칸. 경로상 중간 두 지점 중 하나라도 막히면 못 간다.
- 졸/병(卒/兵): 앞 또는 좌우로 1칸. 뒤로 갈 수 없다.
  - 상대 궁성 안의 대각선 위에서는 대각으로 전진할 수 있다.

## 대국 진행
- 한 수 쉬기(pass) 허용. 스테일메이트는 존재하지 않는다.
- 장군(check)을 받으면 반드시 멍군(해소)해야 한다. 해소 불가 = 외통 = 패배.
- 자기 궁이 장군에 노출되는 수는 둘 수 없다.
- 빅장(양 궁이 같은 줄에서 사이에 기물 없이 마주봄): 기본은 비김. 설정으로 on/off.
- 같은 국면이 반복되면 비김 처리(반복 횟수는 설정값).

## 점수 (기물 점수제)
차 13, 포 7, 마 5, 상 3, 사 3, 졸/병 2, 궁 0.
한 진영은 후수 보상으로 1.5점(덤)을 받는다. 초기 총점은 각 72점.
무승부 조건 발생 시 점수가 높은 쪽이 승리한다.
```

---

## P2. 좌표계와 데이터 모델

```
RULES.md를 근거로 src/engine/types.ts와 src/engine/board.ts를 만들어라.

- Side = 'HAN' | 'CHO', PieceType = 'GUNG'|'SA'|'CHA'|'PO'|'MA'|'SANG'|'JOL'
- Position: { file: 1~9, rank: 1~10 }. 내부 저장은 인덱스 0~89 1차원 배열로 하고 변환 헬퍼를 제공.
- GameState: { board, turn, moveHistory, capturedPieces, config }
- 유틸: isInBoard, isInPalace(pos, side), isOnPalaceDiagonal(pos), forwardDir(side)
- 초기 배치 생성 함수 createInitialBoard(hanSetup, choSetup) — 마상 배치 4종을 인자로 받는다.
- 콘솔에 보드를 텍스트로 찍는 debugPrint(state)도 같이 만들어라.

UI 코드는 아직 만들지 마라.
```

---

## P3. 기물별 이동 생성기

> **기물마다 프롬프트를 나누세요.** 한 번에 7종을 시키면 반드시 어딘가 틀립니다.

```
src/engine/moves/에 기물별 의사이동(pseudo-legal move) 생성기를 만든다.
이번에는 [차(CHA)]만 구현해라.

- 함수 시그니처: generateChaMoves(board, pos): Position[]
- 가로·세로로 빈 칸이면 계속 전진, 아군을 만나면 정지, 적을 만나면 그 칸까지 포함하고 정지.
- 추가로: 자신이 궁성 대각선 위에 있으면 그 대각선 방향으로도 같은 규칙으로 직진 가능.
- 궁성 대각선은 궁성 밖으로 이어지지 않는다.

구현 전에 Vitest 테스트를 먼저 작성해라. 최소 케이스:
1) 빈 보드 중앙에서의 이동 수
2) 아군에 막히는 경우
3) 적 기물을 잡는 경우
4) 궁성 귀퉁이에서 중앙을 지나 반대 귀퉁이까지 가는 경우
5) 궁성 중앙에 기물이 있어 대각 진행이 막히는 경우
```

이후 같은 형식으로 이어서:

- **포**: `사이 기물이 정확히 1개일 때만 이동/공격. 포대가 포이면 불가. 도착점이 포이면 불가.` 테스트에 "포를 넘으려 시도", "포를 잡으려 시도", "사이에 2개" 케이스 필수.
- **마**: 다리 막힘(직선 1칸째 점유) 케이스 필수.
- **상**: 중간 2지점 각각의 막힘 케이스 필수.
- **궁/사**: 궁성 이탈 금지, 대각선 위에서만 대각 이동.
- **졸/병**: 뒤로 금지, 좌우 허용, 상대 궁성 대각 전진 케이스.

---

## P4. 합법수 필터와 장군 판정

```
src/engine/rules.ts를 만든다.

- isAttacked(board, pos, bySide): 해당 지점이 공격받는지. 모든 기물의 이동 생성기를 재사용해라.
- isCheck(state, side): 해당 진영의 궁이 공격받는가.
- generateLegalMoves(state): 의사이동 중 "두고 나서 자기 궁이 장군에 걸리는 수"를 제거한 목록.
- makeMove(state, move): 새 GameState 반환(원본 불변). 잡힌 기물 기록.
- pass(state): 한 수 쉬기.

주의: 포의 공격 판정은 이동 판정과 동일한 규칙(포대 1개)을 쓴다.
따로 구현해서 규칙이 갈라지지 않게 해라.
```

---

## P5. 승패·무승부 판정

```
src/engine/result.ts를 만든다.

- isCheckmate(state, side): 장군 상태이면서 generateLegalMoves가 비어 있으면 외통.
  (한 수 쉬기가 있으므로 "둘 수가 없어서 지는" 스테일메이트는 없다. 장군이 아닌데 수가 없으면 pass한다.)
- isBikjang(state): 양 궁이 같은 file에서 사이에 기물 없이 마주보는 상태. config로 켜고 끌 수 있게.
- 국면 반복 감지: 보드+차례를 해시로 기록해 N회 반복 시 무승부.
- calculateScore(state, side): 차13 포7 마5 상3 사3 졸2, 한 진영에 +1.5.
- getGameResult(state): { status: 'PLAYING'|'CHECKMATE'|'DRAW_BY_SCORE'|..., winner, reason }

이 모듈까지 끝나면 콘솔에서 랜덤 대국이 끝까지 돌아가야 한다.
그 검증용 스크립트도 하나 만들어라.
```

---

## P6. 규칙 검증 (매우 권장)

```
엔진 정확도를 검증하는 테스트를 추가해라.

1) perft: 초기 국면에서 depth 1~3까지 합법수 트리 노드 수를 세는 함수. 회귀 테스트 기준값으로 고정.
2) 랜덤 대국 1000판 자동 실행. 다음이 한 번이라도 발생하면 실패로 처리:
   - 궁이 잡히는 수가 합법수로 생성됨
   - 기물이 보드 밖으로 나감
   - 사/궁이 궁성을 벗어남
   - 포가 포를 넘거나 잡음
   - 졸/병이 뒤로 이동
3) makeMove 후 undo가 원래 상태와 완전히 일치하는지 검사.
```

---

## P7. 보드 렌더링

```
src/ui/Board.tsx를 SVG로 만든다.

- 기물은 선의 교차점 위에 놓인다. 격자의 칸이 아니라 교차점 기준으로 좌표를 계산해라.
- 궁성 두 곳에 X자 대각선을 그린다.
- 기물은 원형에 한자(車包馬象士卒兵將漢楚)를 넣는다. 한은 빨강, 초는 초록.
  한자 대신 한글 표기로 바꿀 수 있는 옵션도 둬라.
- viewBox 기반으로 컨테이너 크기에 맞춰 반응형으로 확대/축소.
- 보드 뒤집기(내 진영을 항상 아래로) 지원.
- props로만 그린다. 이 컴포넌트는 규칙을 전혀 모른다.
```

```
보드에 상호작용을 추가해라.

- 기물 클릭 → 선택 표시 + generateLegalMoves 결과를 점으로 표시
- 이동 가능 지점 클릭 → 이동. 그 외 클릭 → 선택 해제
- 잡을 수 있는 지점은 다른 스타일로 표시
- 마지막 수의 출발·도착 지점 하이라이트
- 장군일 때 해당 궁을 강조
- 드래그 앤 드롭도 지원(모바일 터치 포함)
- 이동 시 CSS transform 트랜지션. prefers-reduced-motion이면 끈다.
```

---

## P8. 게임 흐름과 화면

```
게임 전체 흐름을 붙인다.

1) 대국 설정 화면: 마상 배치 4종 선택(양쪽), 선택 미리보기, 대 AI / 로컬 2인 선택
2) 대국 화면: 보드 + 차례 표시 + 양쪽 점수 + 잡힌 기물 + 기보 목록 + 경과 시간
3) 버튼: 한 수 쉬기, 무르기, 기권, 무승부 제안
4) 종료 다이얼로그: 승패 사유(외통/기권/점수승)와 최종 점수
5) 상태는 useReducer로 관리하고, 액션은 엔진 함수만 호출한다.
```

---

## P9. 기보 저장·재생

```
기보 기능을 만든다.

- 수를 { from, to, piece, captured, isPass } 구조로 저장한다.
- 사람이 읽는 표기는 "출발좌표 + 기물명 + 도착좌표" 형식으로 변환하는 함수를 별도로 둔다.
  표기 규칙은 janggi-notation.ts 한 곳에만 두고 다른 곳에서 문자열을 직접 만들지 마라.
- 전체 대국을 JSON으로 내보내기/불러오기.
- 처음/이전/다음/마지막 버튼으로 기보 리플레이. 리플레이 중에는 착수를 막는다.
- localStorage에 진행 중인 대국을 자동 저장하고, 새로고침 시 복구할지 묻는다.
```

---

## P10. AI 상대 (선택)

```
src/ai/에 장기 AI를 만든다. 엔진 모듈만 import한다.

1단계 — 평가 함수:
- 기물 점수(차13 포7 마5 상3 사3 졸2) + 한 진영 덤 1.5
- 위치 가중치 테이블(졸의 전진, 차의 개방된 줄, 궁성 방어 등)
- 궁의 안전도(궁 주변 공격받는 지점 수)

2단계 — 탐색:
- 알파-베타 가지치기 미니맥스, 반복 심화(iterative deepening)
- 무브 오더링: 잡는 수 우선(MVV-LVA), 킬러 무브
- 시간 제한 기반 중단(기본 1초)
- 트랜스포지션 테이블(Zobrist 해싱)

3단계 — 난이도:
- 초급 depth 2 + 랜덤성, 중급 depth 4, 고급 시간 제한 3초

성능: 탐색은 Web Worker에서 돌려 UI를 막지 않는다. 진행률과 예상 수를 postMessage로 보낸다.
```

---

## P11. 온라인 대전 (선택)

```
실시간 대전 서버를 만든다.

- 서버: Node + TypeScript + ws (또는 Socket.IO)
- 방 생성 / 코드로 입장 / 랜덤 매칭
- 이벤트: create_room, join_room, move, pass, resign, draw_offer, sync_state
- 보안: 서버가 규칙 엔진(동일 코드 공유)으로 모든 수를 재검증한다. 클라이언트를 신뢰하지 않는다.
- 상태 동기화: 서버가 정본. 클라이언트는 낙관적 반영 후 서버 확정으로 교정.
- 재접속: 연결이 끊기면 60초간 자리 유지, 세션 토큰으로 복귀 시 기보 전체 재전송.
- 대국 시계: 피셔 방식 또는 초읽기(예: 10분 + 30초 초읽기 3회). 시간 계산은 서버 기준.
```

---

## P12. 마무리

```
반응형과 접근성을 정리해라.
- 모바일 세로 화면에서 보드가 잘리지 않게 하고, 터치 타깃은 최소 44px.
- 키보드만으로 조작 가능하게(방향키 이동, Enter 선택).
- 각 기물에 aria-label(예: "초 차, 1행 1열").
- 색만으로 진영을 구분하지 않게 형태나 표기도 함께 다르게.
- 색맹 대응 팔레트 옵션.
```

```
배포 준비를 해라.
- 빌드 최적화, AI 워커 코드 스플리팅
- PWA 설정(오프라인에서 로컬 대국 및 AI 대국 가능)
- 에러 바운더리와 상태 복구
- README에 실행 방법, 아키텍처 설명, 규칙 출처 정리
```

---

## 디버깅용 프롬프트 템플릿

```
버그: [증상을 한 줄로]
재현 국면(FEN 또는 debugPrint 출력):
[붙여넣기]
기대: [규칙상 어떻게 되어야 하는가 + RULES.md의 해당 항목]
실제: [무엇이 일어났는가]

먼저 이 국면을 재현하는 실패 테스트를 작성해라.
원인을 설명한 뒤에 고쳐라. 관련 없는 코드는 건드리지 마라.
```

---

## 진행 순서 체크리스트

- [ ] P0 프로젝트 헌법 / P1 RULES.md 고정
- [ ] P2 데이터 모델 + 초기 배치 (콘솔 출력 확인)
- [ ] P3 기물 7종 이동 생성기 (기물마다 개별 프롬프트 + 테스트)
- [ ] P4 합법수 필터, 장군 판정
- [ ] P5 외통·빅장·점수 판정 → **콘솔 랜덤 대국 완주**
- [ ] P6 perft + 랜덤 대국 1000판 검증
- [ ] P7 보드 렌더링 + 상호작용
- [ ] P8 대국 설정/진행 화면 → **여기서 로컬 2인 대국 완성**
- [ ] P9 기보 저장·재생
- [ ] P10 AI (선택) / P11 온라인 (선택)
- [ ] P12 접근성·배포

---

## 진행 팁

1. **P5까지 UI 없이 끝내세요.** 콘솔에서 대국이 완주되기 전에 화면을 만들면 버그 원인이 규칙인지 렌더링인지 구분이 안 됩니다.
2. **규칙 버그는 반드시 테스트부터.** "고쳐줘"라고만 하면 다른 규칙이 깨집니다.
3. **포 규칙을 가장 많이 확인하세요.** 장기 구현에서 오류가 제일 잦은 기물입니다.
4. 새 세션을 시작할 때마다 RULES.md를 다시 읽히세요.
5. AI 상대는 마지막에. 규칙 엔진이 틀리면 AI는 그 틀린 규칙을 열심히 최적화합니다.
